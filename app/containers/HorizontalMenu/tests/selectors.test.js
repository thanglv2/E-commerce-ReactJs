// import { selectHorizontalMenuDomain } from '../selectors';

describe('selectHorizontalMenuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
