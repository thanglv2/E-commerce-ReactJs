/*
 * HorizontalMenu Messages
 *
 * This contains all the text for the HorizontalMenu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HorizontalMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HorizontalMenu container!',
  },
});
