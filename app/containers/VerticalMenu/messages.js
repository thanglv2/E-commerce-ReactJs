/*
 * VerticalMenu Messages
 *
 * This contains all the text for the VerticalMenu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VerticalMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerticalMenu container!',
  },
});
