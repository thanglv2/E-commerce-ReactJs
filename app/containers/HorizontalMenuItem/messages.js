/*
 * HorizontalMenuItem Messages
 *
 * This contains all the text for the HorizontalMenuItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HorizontalMenuItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HorizontalMenuItem container!',
  },
});
