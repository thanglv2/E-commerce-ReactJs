// import { selectHorizontalMenuItemDomain } from '../selectors';

describe('selectHorizontalMenuItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
