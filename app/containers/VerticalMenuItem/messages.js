/*
 * VerticalMenuItem Messages
 *
 * This contains all the text for the VerticalMenuItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VerticalMenuItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerticalMenuItem container!',
  },
});
