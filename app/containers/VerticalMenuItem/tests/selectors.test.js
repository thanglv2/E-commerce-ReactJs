// import { selectVerticalMenuItemDomain } from '../selectors';

describe('selectVerticalMenuItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
