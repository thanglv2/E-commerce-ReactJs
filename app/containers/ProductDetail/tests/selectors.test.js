// import { selectProductDetailDomain } from '../selectors';

describe('selectProductDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
